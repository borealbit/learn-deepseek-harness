# Synthetic Release Candidate

This deliberately small package is inspected by the Module 12 capstone. It is
not published and its `example.invalid` repository URL is intentionally inert.

The fixture exports one deterministic function. Its lint and test commands are
read-only. Its build command writes one declared file under `dist/` and must not
run without one-shot approval.

