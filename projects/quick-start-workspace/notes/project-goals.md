# Project Goals

The public codename for this exercise is **Borealis**.

The project should remain deliberately small:

- one greeting module;
- no external dependencies;
- no build step;
- no credential or network access; and
- enough documentation for a read-only repository summary.

The first-run exercise succeeds when a learner can explain the fixture and detect any disagreement between its documents without changing a file.
